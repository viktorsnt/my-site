%section#social
	
	.container

		%nav
			%ul
				- data.settings.social.each do |social|
					= social_network_link(social.name, social.url)

%section#contact

	.container

		- data.settings.contact.each do |contact|
			%div{lang: "#{contact.lang}"}
				%h2= contact.title
				.contact-inner
					%p= contact.text

		%h3.text-center= data.settings.email
		
		%br
		%br
		%br
		%br
		%br
		%br
		%br
		
		/ 	%form
		/ 		.form-group
		/ 			/ %label{for: "name"} Name
		/ 			%input#name{type: "text", placeholder: "Name"}
		/ 		.form-group
		/ 			/ %label{for: "email"} Email
		/ 			%input#email{type: "text", placeholder: "Email"}
		/ 		.form-group
		/ 			/ %label{for: "regularTextarea"} Message
		/ 			%textarea#regularTextarea{placeholder: "Message", rows: 5}
		/ 		.form-group
		/ 			%button{type: "submit"} Send